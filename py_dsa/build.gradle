apply from: "${rootDir}/gradle/scripts/py.gradle"

install {
    dependsOn("${project.path}:pyDeps")
}

task check {
    dependsOn("${project.path}:pyCheck")
}

task format {
    dependsOn("${project.path}:pyFormat")
}
